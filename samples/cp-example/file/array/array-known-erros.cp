// array-known-errors.cp

var arrValue : int = {1,2,3}; // deveria dar erro ao declarar array em um int

print(arrValue[0]);
print("\n");


var strArray[][] : int = {{0,1},{1,0}};

print(strArray[0][1]); // 
print("\n\n");

