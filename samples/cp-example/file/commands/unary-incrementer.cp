// unary-incrementer

var arr[] : int = { 1, 2, 3 };

for (var i : int = 0; i < len(arr); i++) {
  print(arr[i]);
}

print('\n');

for (var i : int = len(arr) - 1; i >= 0; i--) {
  print(arr[i]);
}

print('\n');
