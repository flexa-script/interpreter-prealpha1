var smls = `test`;

println(smls);

var b = true;
var i = 10;
var s = "this is a string in a variable";

var mls = `
This is a multiline string!
This is a new line.
• Boolean:\t${b}
• Integer:\t${s}
• String:\t${s}
`;

println('<', mls, '>');
