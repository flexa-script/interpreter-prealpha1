//print("Hello World!\n");
// for
// corrigir nodes que não tinham acesso a array como expr
// adicionar expr na decl

var arr[] : int = { 1, 2, 3 };

for (var i : int = 0; i < len(arr); i = i + 1) {
  print(arr[i]);
}

print('\n');

for (;;) {
  print("loop");
  break;
}

print('\n');

// foreach (int val : arr) {
//     print(arr);
// }
