//print("Hello World!\n");
// for

var arr : int = { 1, 2, 3 };

for (var i : int = 0; i <= len(arr); i = i + 1) {
  print(arr[i]);
}

for (;;) {
  print("loop");´
  break;
}

// foreach (int val : arr) {
//     print(arr);
// }
