
// const MAX_VALUE = 99999999; //18446744073709551615

// struct Hashtable {
// 	var used_keys: List;
// 	var keys[MAX_VALUE]: string;
// 	var values[MAX_VALUE]: any;
// };

// var x = Hashtable{values={}};

// println(len(x.keys));
// println(len(x.values));

// x.values[0] = 10;

// println(x.values[0]);

var x = {10, '5', "asda"};

println(x);
