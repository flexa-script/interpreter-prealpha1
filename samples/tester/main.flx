using utils;
using flx.std.testing;

// include namespace flx; // shouldn't call from flx namespace if it's commented

var gsw: StopWatch = StopWatch{};

timer_start(gsw);

using test_suite;

//execute_tests();

timer_stop(gsw);
print("full test suite elapsed time: ", gsw.elapsed_time, " ms");

println('\n');
