using bsl.std.structs;
using structs.definition;

include namespace bsl;

var f = Foo{x=1, y=2};
// var f = Foo{name="Carlos", age = 29, bar = null};

foreach (var it: Pair in f) {
	print("key: ", it.key, "\nvalue: ", it.value, "\ntype: ", typeof(it), '\n');
}
