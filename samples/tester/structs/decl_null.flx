struct Element_decl_null {
    var has_value;
    var value;
};

var element : Element_decl_null;

element.has_value = true;
element.value = 10;

print(element.has_value);
print('\n');
print(element.value);
print('\n');
