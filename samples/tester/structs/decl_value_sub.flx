struct ElementX {
  var has_value : bool;
  var value : any;
  var next : ElementX;
};

struct List{
  var root : ElementX;
};

var list : List;

list.root = ElementX {
  has_value = true,
  value = 10
};

print(list.root.has_value);
print('\n');
print(list.root.value);
print('\n');
